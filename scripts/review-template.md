## Review << Feature Description >>
Name: << feature name as found in test `testing` attribute, and summary table >>
Date: << review date >>
Test Count: << number of tests >>

### Specification Extract 
Header in specification: << exact header text from index.md that matches the function/operator/feature being reviewed >>

<< Extract markdown from the specification, and copy here - local copy to reduce context size for processing >>

### Example(s) from Specification
<< Where the specification has examples, copy those here >>

### Coverage
<< `n` tests found (comma seperated list of tests). >>

**Covered:**
- << ✅ Feature tested (testName) >>

**Gaps:**
- << ❌ component of feature not covered by a test >>

### Test Results

<< Test results table >>

**Summary:** n/total (%) — x tests × y engines

<< 
An overall function status summary indicating overall the state of the function and how consistent the results are across engines (but not naming them).
If there are failing tests, suggest if this might be due to issues with the tests, or with the engines, or with the specification, based on the patterns of which tests are failing across engines.
e.g. 10 test(s) fail in only one engine, suggesting engine-specific implementation issues rather than problems with the tests or specification. 1 test(s) fail in multiple (but not all) engines, which may indicate differing interpretations of the specification.
>>
